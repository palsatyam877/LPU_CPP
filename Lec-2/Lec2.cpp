#include<bits/stdc++.h>
using namespace std;

int main() {
    
    // a += 7;
    // cout << a << "\n";

    // a -= 2;
    // cout << a << "\n";
    

    // a *= 5;
    // cout << a << "\n";
    
    // a /= 5;
    // cout << a << "\n";
    
    // a %= 2;
    // cout << a << "\n";

    // int a = 5;

    // int x = (a > 2 < 1) ? 1 : 0;

    // cout << x << "\n";

    int cnt = 100;

    for(int i = 100; i >= 0 && i >= 1; --i) {
        cout << i << "\n";
    }
    
    return 0;
}